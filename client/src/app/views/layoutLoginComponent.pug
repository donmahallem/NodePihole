.mainbox.col-md-6.col-md-offset-3.col-sm-6.col-sm-offset-3
	.panel.panel-default
		.panel-heading
			div(style='text-align: center;')
				img(src='static/img/logo.svg', width='30%')
			br
			.panel-title.text-center
				span.logo-lg(style='font-size: 25px;')
					b Pi
					| -hole
			p.login-box-msg Sign in to start your session
			.form-group.has-error.login-box-msg(ng-if="$ctrl.wrongPassword")
				label.control-label
					i.fa.fa-times-circle-o
					|  Wrong password!
		.panel-body
			form(ng-submit="$ctrl.login($ctrl.password)")
				.form-group.has-feedback(ng-class="{'has-error':$ctrl.wrongPassword}")
					input.form-control(type='password', ng-disabled="$ctrl.isRequesting", name='pw', placeholder='Password', autofocus='',ng-model="$ctrl.password")
					span.glyphicon.glyphicon-lock.form-control-feedback
				.row
					.col-xs-8
						ul
							li
								tt Return
								|  → Log in and go to requested page (
								| )
							li
								tt Ctrl+Return
								|  → Log in and go to Settings page
					.col-xs-4
						button.btn.btn-primary.pull-right(type='submit', ng-disabled="$ctrl.isRequesting" href='#')
							i.glyphicon.glyphicon-log-in
							|    Log in
				br
				.row
					.col-xs-12
						.box.box-solid(ng-class="$ctrl.wrongPassword?'box-danger':'box-info collapsed-box'")
							.box-header.with-border
								h3.box-title Forgot password
								.box-tools.pull-right
									button.btn.btn-box-tool(type='button', data-widget='collapse')
										i.fa(ng-class="$ctrl.wrongPassword?'fa-minus':'fa-plus'")
							.box-body
								| After installing Pi-Hole for the first time, a password is generated and displayed to the user. The
								| password cannot be retrived later on, but it is possible to set a new password (or explicitly disable
								| the
								| password by setting an empty password) using the command
								pre.
									\nsudo pihole -a -p newpassword									
